name: Deploy Java backend

on:
  push:
    branches:
      - master


jobs:
  deploy:
    runs-on: [self-hosted, Windows, X64, server-19]
    if: contains(github.event.head_commit.message, '#deploy')
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'corretto'


      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Login GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.CR_PAT }}

      - name: Build Docker image
        run: |
            docker build  --no-cache -t ghcr.io/wsnsa/dis-news-service:latest .

      - name: Push Docker image
        run: docker push --disable-content-trust=true ghcr.io/wsnsa/dis-news-service:latest

      - name: Pull Docker image
        run: |
          docker pull ghcr.io/wsnsa/dis-news-service:latest

      - name: Stop and remove existing container
        run: |
          docker ps --quiet --filter "name=dis-backend" | ForEach-Object { docker rm -f $_ }

      - name: Run Docker container
        run: |
          docker run -d -p 8083:8083 --restart always --name dis-backend `
            -e SPRING_DATASOURCE_URL="jdbc:mysql://host.docker.internal:3306/dis_news?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ulaanbaatar" `
            -e SPRING_DATASOURCE_USERNAME="root" `
            -e SPRING_DATASOURCE_PASSWORD="root123" `
            ghcr.io/wsnsa/dis-news-service:latest

      - name: Cleanup old Docker images
        run: |
          docker image prune -f


